#include "features/ble_wifi_provisioning/Registration.h"

namespace features::ble_wifi_provisioning {

void registerFeature() {
  // Intentionally empty: BLE provisioning is wired through capability checks
  // and runtime services rather than feature-local registries.
}

}  // namespace features::ble_wifi_provisioning
